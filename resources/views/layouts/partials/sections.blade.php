
    <div class="tabs">
      <div class="tabs-labels mb-3">
        <a href="{{route('home')}}" class="@yield('active-run')" for="home">Run Metric</a>
        <a href="{{route('history')}}" class="@yield('active-history')" for="history">Metric History</a>
      </div>
    </div>
